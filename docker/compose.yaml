# Run the full stack from the repo root instead:
#   docker compose up -d
#
# This file remains for backward compatibility when working inside docker/.
# Paths in the included compose file resolve relative to the repo root.

include:
  - path: ../docker-compose.yml
